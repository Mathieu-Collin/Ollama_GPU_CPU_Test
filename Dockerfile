# Optional Dockerfile format (not required by docker compose in this setup)
# Provided to match requested filenames. The compose uses the official image directly.
FROM ollama/ollama:latest

EXPOSE 11434


